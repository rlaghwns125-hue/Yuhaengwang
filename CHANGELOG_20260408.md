# 유행왕 작업 로그 - 2026-04-08

## 1. 마켓 등급 시스템

### 상품 등급 (Rarity) 추가
- `MarketItem`에 `rarity` 필드 추가: `normal`, `rare`, `epic`, `unique`, `legend`
- 등급별 가격: 500 / 1,000 / 2,000 / 5,000 / 10,000 쿠키
- 등급별 색상 뱃지 (회색/파랑/보라/금색/빨강)
- 마켓 그리드에 등급 뱃지 + 등급 색 가격 표시
- 기존 상품 등급 적용: 미드나잇 네온(에픽), 레드핑크/피톤치드그린(레어), 아이콘 전체(노멀)

### 디저트바 파스텔 톤 변경
- 레드핑크: 원색 → 파스텔 핑크 (#FFE4EB / #FFD1DC / #FF8FAB)
- 피톤치드그린: 원색 → 파스텔 민트 (#DCF5E6 / #C1E8D5 / #7BC8A4)

## 2. 관리자 페이지 (`/market-admin`)

### 신규 생성
- `rlaghwns125@gmail.com` 전용 (마켓 헤더 "관리" 버튼)
- 카테고리별 탭: 디저트바 / 아이콘 / 가게리스트 / 챗봇

### 카테고리별 전용 입력 폼 (JSON 직접 입력 제거)
- **디저트바**: 바 배경색, 버튼 배경색, 선택 배경색, 텍스트색, 선택 텍스트색, 모서리 둥글기
- **아이콘**: 적용 키워드, 이미지 첨부 (base64, 500KB 이하)
- **챗봇**: 아이콘 이미지, 페르소나, 말투 접미사, 말풍선 색, 시스템 프롬프트
- **가게리스트**: 배경색, 텍스트색, 강조색

### 색상 선택 → 네이티브 RGB 컬러피커
- `<input type="color">` 사용 — 마우스로 색상 선택 + HEX 직접 입력 병행

### 실시간 미리보기
- 디저트바: 상점 카드와 동일한 형태 (배경 + "유행왕" 버튼)
- 아이콘: assets 이미지 또는 업로드 이미지 (100x100)
- 챗봇: 아이콘 + 말풍선 대화 미리보기
- 가게리스트: 가게 목록 3줄 미리보기

### 상품 수정 기능
- 상품 목록에서 "수정" 버튼 → 폼에 기존 데이터 자동 채움 (금색 테두리)
- 등급/이름/설명/스타일 전부 수정 가능
- 탭 전환 시 수정 모드 자동 해제

### 상품 미리보기 개선
- 이모지 제거 → 카테고리별 실제 디자인으로 표시
- 아이콘: `getDessertPremiumImage(keyword)`로 assets 이미지 매칭

### Firestore 기반 상품 관리
- `marketItems` 컬렉션에 상품 저장
- `marketStore.loadMarketItems()` — Firestore에서 상품 로드 (하드코딩 폴백)
- 기존 10개 상품 Firestore 시드 완료 (`scripts/seed-market-items.js`)
- 관리자가 등록/수정/삭제하면 바로 반영

## 3. 디저트 아이콘 시스템 변경

### 기본 아이콘 → 전부 이모지로 복원
- 마카롱(🧁), 브라우니(🍫), 크로플(🧇), 붕어빵(🐟), 탕후루(🍡), 약과(🍘), 빙수(🍧)
- PNG 이미지는 마켓 구매 시에만 적용 (프리미엄 아이콘)

### 마켓 아이콘 탭
- 7개 디저트 아이콘 상품 등록 (각 500쿠키, 노멀 등급)
- 구매 = 자동 적용 (장착/해제 없음)
- TrendButtons에서 `purchasedIds` 구독 → 구매한 키워드만 이미지로 표시

## 4. 검색 정확도 개선

### 네이버 검색 결과 엄격 필터
- 가게명에 검색 키워드가 포함되지 않으면 결과에서 제외
- "우베" 검색 시 "노티드 여의도IFC몰" 같은 관련 없는 결과 제거

### 네이버 지도 연결 URL 간소화
- 변경 전: `서울특별시 가게이름 영등포구 당산로 00-00번길 0`
- 변경 후: `서울특별시 가게이름` (시/도 + 가게명만)

## 5. TOP3 초기화 기준 변경

### 24시간 롤링 → 매일 00시 초기화
- `getPopularSearches()` — `where('searchedAt', '>=', 오늘 00:00)` Firestore 쿼리
- 자정 지나면 자동으로 새 카운트 시작

### 오래된 로그 정리 스크립트
- `scripts/cleanup-search-logs.js` — 기본 24시간 이전 로그 삭제

## 6. 마켓 흰 화면 문제 수정

### React Error #310 해결
- 원인: 조건부 return 전후로 훅 호출 순서 불일치 (`addCookies` 등)
- 수정: 모든 훅을 조건부 return 위에 선언

### authReady 타임아웃
- `_layout.tsx` — 5초 안에 authReady 안 되면 강제 설정
- `market.tsx` — 3초 타임아웃 + 100ms 폴링, 비로그인 확정 시 메인 리다이렉트

## 7. 트렌드 자동 업데이트 스케줄러

### Windows 작업 스케줄러 등록
- 매일 00:00 실행 (1일 1회)
- PC 꺼져있다 켜지면 놓친 실행 즉시 보충 (`StartWhenAvailable`)
- 실행 제한 10분 (`ExecutionTimeLimit`)

### 로그 날짜별 관리
- `scripts/run-trends.bat` — 래퍼 스크립트
- 로그 경로: `scripts/logs/년도/월/일.log`
- 실행 로그에 시작/종료 시간 표시

### 데이터랩 조회 설정 변경
- 조회 기간: 30일 → **7일**
- 조회 단위: 주간(week) → **일간(date)**

## 8. 기타

### 검색 로그 쿨다운 변경
- 1분 → **10분** (디바이스당 같은 키워드)
- `logSearch` 호출 시점: 검색 완료 후 → **검색 시작 직후** (검색 실패해도 로그 저장)

### 검색 로그 집계 스크립트 개선
```bash
node scripts/check-search-logs.js        # 오늘 00시 이후 (기본)
node scripts/check-search-logs.js 48     # 최근 48시간
node scripts/check-search-logs.js all    # 전체
```

### 마켓 아이콘 교체
- 이모지(🏪) → 커스텀 이미지 (`assets/icons/market.png` — 장바구니+쿠키)

### 디저트바 테마 적용 범위 확장
- 헤더(topOverlay): 배경색, 지역명 텍스트, 랭킹 뱃지
- 마켓/마이페이지 버튼: 배경색, 텍스트색
